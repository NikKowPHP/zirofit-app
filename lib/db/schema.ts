import schemaV5 from './migrations/schemaV5'

// Central export for the active WatermelonDB schema.
// We currently use schema version 5 defined in `migrations/schemaV5`.
export const mySchema = schemaV5
