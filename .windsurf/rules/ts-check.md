---
trigger: model_decision
description: check and fix TypeScript errors after every code modification using the npm run ts-check script.
---

After any code modification:

- Run the command `npm run ts-check`
- If issues are found fix them sequentially until no issues found 
- Confirm that all TypeScript errors fixed
